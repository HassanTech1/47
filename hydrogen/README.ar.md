# دليل متجر 4Seven's على Shopify Hydrogen — بالعربي 🇸🇦

## ما هو Shopify Hydrogen؟

**Hydrogen** هو إطار عمل رسمي من Shopify لبناء متاجر إلكترونية سريعة ومتقدمة.  
بدلاً من استخدام واجهة Shopify العادية، يمكنك تصميم متجرك بالكامل بنفسك باستخدام React — مع بيانات حقيقية من Shopify مباشرةً.

---

## 🗂️ هيكل المشروع

```
hydrogen/
├── server.ts              ← نقطة الدخول الرئيسية (Cloudflare Worker)
├── app/
│   ├── root.jsx           ← الصفحة الأساسية (HTML + CSS + Outlet)
│   ├── routes/
│   │   ├── _index.jsx         ← الصفحة الرئيسية
│   │   ├── products.$handle.jsx   ← صفحة تفاصيل المنتج
│   │   ├── collections.$handle.jsx ← صفحة الكوليكشن
│   │   ├── cart.jsx           ← صفحة السلة
│   │   └── account.jsx        ← صفحة الحساب
│   ├── components/
│   │   ├── Header.jsx         ← الهيدر (الشريط العلوي)
│   │   ├── Footer.jsx         ← الفوتر
│   │   ├── HeroSection.jsx    ← قسم الهيرو (أول شيء يظهر)
│   │   ├── ProductGrid.jsx    ← شبكة المنتجات
│   │   ├── CartDrawer.jsx     ← درج السلة الجانبي
│   │   ├── PromoBanner.jsx    ← بانر العروض
│   │   └── LifestyleSection.jsx ← قسم الصور الجمالية
│   ├── lib/
│   │   ├── context.ts         ← الاتصال بـ Shopify API
│   │   ├── session.ts         ← إدارة الجلسة (Cookies)
│   │   ├── fragments.ts       ← GraphQL للسلة
│   │   └── mock-data.js       ← بيانات تجريبية عند الغياب عن الإنترنت
│   └── styles/
│       └── app.css            ← التصميم العام + Tailwind
├── .env.example           ← نموذج متغيرات البيئة
└── vite.config.js         ← إعدادات البناء
```

---

## ⚙️ خطوات الإعداد المحلي (على جهازك)

### 1. انسخ مجلد المشروع

```bash
cd hydrogen
```

### 2. انسخ ملف البيئة

```bash
cp .env.example .env
```

### 3. افتح ملف `.env` وأضف بيانات متجرك

```env
# سر عشوائي لتأمين الجلسة
SESSION_SECRET=اكتب_هنا_سر_عشوائي_طويل

# دومين متجرك على Shopify
PUBLIC_STORE_DOMAIN=اسم-متجرك.myshopify.com
PUBLIC_CHECKOUT_DOMAIN=اسم-متجرك.myshopify.com

# توكن Storefront API العام (من لوحة تحكم Shopify)
PUBLIC_STOREFRONT_API_TOKEN=انسخ_التوكن_العام_من_لوحة_التحكم

# توكن Storefront API الخاص (للسيرفر فقط)
PRIVATE_STOREFRONT_API_TOKEN=انسخ_التوكن_الخاص_من_لوحة_التحكم
```

> ⚠️ **مهم:** لا ترفع ملف `.env` على GitHub — هو مضاف في `.gitignore` تلقائياً.

### 4. ثبّت المكتبات

```bash
npm install
```

### 5. شغّل المشروع محلياً

```bash
npm run dev
```

افتح المتصفح على: **http://localhost:3000**

---

## 🔑 كيف أحصل على بيانات Shopify API؟

1. اذهب إلى **Shopify Admin** (لوحة تحكم متجرك)
2. من القائمة: **Apps** ← **Develop apps**
3. اضغط **Create an app** أو افتح التطبيق الموجود
4. اضغط **Configure Storefront API scopes** وفعّل:
   - `unauthenticated_read_product_listings`
   - `unauthenticated_read_product_inventory`
   - `unauthenticated_write_checkouts`
   - `unauthenticated_read_checkouts`
5. اضغط **Save** ثم اذهب إلى **API credentials**
6. انسخ:
   - **Storefront API public access token** ← ضعه في `PUBLIC_STOREFRONT_API_TOKEN`
   - **Admin API secret key** ← ضعه في `PRIVATE_STOREFRONT_API_TOKEN`

---

## 🚀 النشر على Shopify Oxygen

**Oxygen** هو خدمة الاستضافة الرسمية من Shopify — مجانية ومتكاملة.

### الطريقة الأولى: عبر سطر الأوامر

```bash
cd hydrogen
npm run build
npx shopify hydrogen deploy
```

### الطريقة الثانية: ربط GitHub تلقائياً (الأسهل)

1. اذهب إلى **Shopify Partner Dashboard**
2. اختر متجرك ← **Hydrogen storefronts**
3. اضغط **Create storefront**
4. اختر **Connect GitHub repository**
5. اختر المستودع `HassanTech1/47`
6. اختر الـ branch: `copilot/deploy-shopify-hydrogen-site`
7. اضغط **Deploy**

كل مرة تدفع تعديلات على GitHub، Shopify ينشرها تلقائياً ✅

---

## 🛍️ كيف تضيف منتجاتك الحقيقية؟

1. اذهب إلى **Shopify Admin** ← **Products**
2. أضف منتجاتك مع الصور والأسعار والأحجام
3. المتجر سيجلب المنتجات تلقائياً عبر Storefront API

> إذا لم يكن هناك منتجات في Shopify، المتجر يعرض **بيانات تجريبية** من ملف `mock-data.js` تلقائياً.

---

## 📦 بناء المشروع

```bash
npm run build
```

ينتج مجلد `dist/` بداخله:
- `dist/client/` ← ملفات الواجهة (JavaScript + CSS)
- `dist/server/` ← ملف السيرفر (Cloudflare Worker)

---

## 🧩 الصفحات والمسارات

| الرابط | الملف | الوصف |
|--------|-------|-------|
| `/` | `routes/_index.jsx` | الصفحة الرئيسية |
| `/products/اسم-المنتج` | `routes/products.$handle.jsx` | تفاصيل المنتج |
| `/collections/اسم-الكوليكشن` | `routes/collections.$handle.jsx` | صفحة الكوليكشن |
| `/cart` | `routes/cart.jsx` | صفحة السلة |
| `/account` | `routes/account.jsx` | صفحة الحساب |

---

## 🔧 التعديل على التصميم

- **الألوان والخطوط:** عدّل `app/styles/app.css`
- **تصميم المنتجات:** عدّل `app/components/ProductGrid.jsx`
- **الهيدر:** عدّل `app/components/Header.jsx`
- **الصفحة الرئيسية:** عدّل `app/routes/_index.jsx`

---

## ❓ الأسئلة الشائعة

**س: المتجر يعمل بدون ربط Shopify؟**  
ج: نعم! يعرض بيانات تجريبية تلقائياً من `mock-data.js`.

**س: كيف أغير اللغة إلى العربية كاملاً؟**  
ج: في `app/lib/context.ts` غيّر `language: 'EN'` إلى `language: 'AR'`، وفي `app/root.jsx` غيّر `lang="en"` إلى `lang="ar"` مع إضافة `dir="rtl"`.

**س: كيف أضيف صفحة جديدة؟**  
ج: أنشئ ملف جديد في `app/routes/` باسم الرابط المطلوب، مثلاً `about.jsx` للوصول عبر `/about`.

**س: أين أضع شعار المتجر؟**  
ج: في `app/components/Header.jsx` — استبدل النص `7777` بصورة `<img>` تشير إلى شعارك.
