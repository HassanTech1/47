# This folder is populated by `npm run build:shopify` (or `yarn build:shopify`) from the frontend/ directory.
# The build outputs theme-bundle.js and theme-bundle.css here, along with any image/font assets.
# These files are required by layout/theme.liquid for the Shopify theme to function.
